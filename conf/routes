# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.SearchRequestController.testConnection
POST    /request                    controllers.SearchRequestController.save
PUT     /request/:id                controllers.SearchRequestController.update(id: String)
GET     /request/:id                controllers.SearchRequestController.find(id: String)
DELETE  /request/:id                controllers.SearchRequestController.remove(id: String)

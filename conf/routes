# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.ClientSearchController.testConnection
GET     /request/:id                controllers.ClientSearchController.find(id: String)
DELETE  /request/:id                controllers.ClientSearchController.remove(id: String)
